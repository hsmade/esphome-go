name: Test
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: test
        run: |
          go mod tidy
          go mod vendor
          go test ./...
          go vet ./...
